# 🔗 Что такое React‑ссылка (ref)? Почему не нужно использовать поиск DOM через `document`

---

## Определение

- **Ref** — это «escape hatch» React, позволяющая получить **императивный доступ** к DOM‑узлам или сохранённым значениям без разрыва декларативного потока.  
- Создаётся двумя способами:  
  - **`useRef(initialValue)`** в функциональных компонентах;  
  - **`React.createRef()`** в классовых.  
- Реф — это простой объект `{ current: … }`, куда React помещает ссылку на DOM‑элемент или компонент.

---

## Зачем использовать ref вместо `document.querySelector`

|                              | Ref (`useRef` / `createRef`)                                                      | `document.querySelector` / прямой доступ к DOM        |
|------------------------------|-----------------------------------------------------------------------------------|--------------------------------------------------------|
| **Жизненный цикл React**     | React гарантирует установку/сброс `ref.current` при монтировании/размонтировании  | Может возвращать устаревший элемент, уже удалённый React |
| **Изоляция компонентов**     | Реф локален для компонента, не зависит от глобального состояния DOM               | Глобальный поиск по документу — нечувствителен к иерархии React |
| **Server‑Side Rendering**    | Во время SSR `ref.current` просто остаётся `null` без ошибок                      | `document` недоступен — приведёт к ошибке               |
| **Тестирование**             | Легко мокируется и изолируется при unit‑тестах                                    | Труднее эмулировать в тестовой среде                   |
| **Производительность**       | React делегирует обновление `ref.current` оптимизированно                         | Постоянные обращения к `document` обходят React и медленнее |

---

## Пример использования

```jsx
import { useRef } from 'react';

function TextInputWithFocusButton() {
  const inputRef = useRef(null);

  function handleClick() {
    // React установил current в <input> после монтирования
    inputRef.current.focus();
  }

  return (
    <>
      <input ref={inputRef} type="text" />
      <button onClick={handleClick}>Focus input</button>
    </>
  );
}
```

- Здесь `inputRef.current` всегда ссылается на актуальный DOM-элемент.
- Не нужно искать элемент в документе по селектору — вы точно работаете с тем узлом, который рендерит этот компонент.

---

## Итог

- **Refs** — правильный способ императивного доступа к DOM или сохранению значений между рендерами.
- Прямой поиск через `document.*` **нарушает** жизненный цикл React, глобально захламляет код и может привести к ошибкам в SSR и тестах.
- Используйте **`useRef` / `createRef`**, чтобы оставаться в рамках React‑потока и гарантировать корректность ссылок.
